# Application Configuration
PORT=3001
NODE_ENV=development

# Database Configuration - SQLite (Default)
# Không cần cài đặt gì, chỉ cần file database
DB_TYPE=sqlite
DB_DATABASE=dev.db

# Database Configuration - Postgres (VD: Vercel Postgres)
# Khi tạo database trên Vercel (Storage -> Postgres), Vercel sẽ tự động thêm các biến môi trường:
# POSTGRES_URL, POSTGRES_USER, POSTGRES_HOST, POSTGRES_PASSWORD, POSTGRES_DATABASE
# Bạn KHÔNG CẦN copy thủ công nếu đã link project với database trên Vercel.
# Tuy nhiên, bạn CẦN thêm biến này thủ công trong phần Environment Variables trên Vercel:
# DB_TYPE=postgres
# DB_SYNCHRONIZE=true  <-- QUAN TRỌNG: Set là 'true' cho lần deploy đầu tiên để tạo bảng, sau đó đổi lại 'false'
# DB_SSL=true

# Nếu chạy local với Postgres, điền các thông tin dưới:
DATABASE_URL=
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=
DB_DATABASE=motobiketours
DB_SSL=false
DB_SYNCHRONIZE=true
DB_LOGGING=true

# Cloudinary Configuration (Cho upload ảnh)
# Lấy thông tin từ: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# JWT Configuration (Cho authentication)
JWT_SECRET=your_super_secret_key_change_this_in_production
JWT_EXPIRATION=1d
